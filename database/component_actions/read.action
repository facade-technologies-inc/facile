name = "Read Text"
description = "Reads text from component {id}."
targets = ['Edit', 'Static']
inputs = []
outputs = [
	{
		"name": "value",
		"description": "Value obtained from reading component text.",
		"optional": False,
		"data type": str,
	}
]
code = """
return comp.get_value()
"""